/**
 * Configurações — Wrapper
 * A lógica real está em /js/settings.js
 */

document.addEventListener('DOMContentLoaded', function () {
    // O settings.js já cuida de tudo
    // Este arquivo só garante que os elementos da página estejam prontos
    console.log('📄 Página de configurações carregada');
});
